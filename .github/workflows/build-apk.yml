name: Build APK
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:作业:
  build:构建:
    runs-on: ubuntu-latest运行环境: ubuntu-latest
    steps:步骤:
      - uses: actions/checkout@v4      - 使用: actions/checkout@v4 - 使用: actions/checkout@v4 - 使用: actions/checkout@v4 - 使用: actions/checkout@v4
      - uses: subosito/flutter-action@v2      - 使用: subosito/flutter-action@v2
        with:使用:
          flutter-version: '3.24.0'Flutter 版本: '3.24.0'
      - run: flutter pub get      - 运行: flutter pub get
      - run: flutter build apk --release      - 运行: flutter build apk --release
      - uses: actions/upload-artifact@v4      - 使用: actions/upload-artifact@v4
        with:使用:
          name: app-release-apk名称: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk路径: build/app/outputs/flutter-apk/app-release.apk路径: bbuild/app/outputs/flutter-apk/app-release.apk路径: build/app/outputs/flutter-apk/app-release.apk路径: build/app/outputs/flutter-apk/app-release.apk
